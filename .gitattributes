# Normalize line endings across platforms
* text=auto

# Ensure C++ files are treated as text with proper diff
*.cpp text diff=cpp
*.h text diff=cpp

# Binary files
*.o binary
*.a binary
*.so binary
*.dylib binary
*.dll binary

# Ensure shell scripts are treated with LF line endings
*.sh text eol=lf
