# The LPSCoin-Qt code is licensed under MIT, which is compatible with OpenSSL.
# Only the Debian packaging scripts are GPL-2+, posing no licensing conflict.
lpscoins-qt: possible-gpl-code-linked-with-openssl
